import { AutodocRepoConfig } from '../../../types.js';
export declare const index: ({ name, repositoryUrl, root, output, llms, ignore, filePrompt, folderPrompt, chatPrompt, contentType, targetAudience, linkHosted, }: AutodocRepoConfig) => Promise<void>;
declare const _default: {
    index: ({ name, repositoryUrl, root, output, llms, ignore, filePrompt, folderPrompt, chatPrompt, contentType, targetAudience, linkHosted, }: AutodocRepoConfig) => Promise<void>;
};
export default _default;
