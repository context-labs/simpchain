import { AutodocRepoConfig } from '../../../types.js';
export declare const estimate: ({ name, repositoryUrl, root, output, llms, ignore, filePrompt, folderPrompt, chatPrompt, contentType, targetAudience, linkHosted, }: AutodocRepoConfig) => Promise<void>;
